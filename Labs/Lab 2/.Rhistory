ChiMarkets.54019 = st_transform(ChiMarkets, 54019)
library(sf)
ChiMarkets.54019 = st_transform(ChiMarkets, 54019)
##Q3
#A.
filter(us_states, REGION == "Midwest")
library(sf)
library(dplyr)
library(terra)
library(spData)
data(us_states)
data(us_states_df)
##Q3
#A.
filter(us_states, REGION == "Midwest")
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp <- world %>%
group_by(continent) %>%       # Group the data by continent
summarise(
median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group
)
# View the results
print(median_life_exp)
library(dplyr)
median_life_exp = world %>%
group_by(continent) %>%
summarise(median_life_expectancy = median(lifeExp)
print(median_life_exp)
library(dplyr)
median_life_exp = world %>%
group_by(continent) %>%
summarise(median_life_expectancy = median(lifeExp)
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp = world %>%
group_by(continent) %>%
summarise(median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group)
# View the results
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp <- world %>%
group_by(continent) %>%       # Group the data by continent
summarise(
median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group
)
# View the results
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp = world %>%
group_by(continent) %>%       # Group the data by continent
summarise(
median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group
)
# View the results
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp = world %>%
group_by(continent) %>%       # Group the data by continent
summarise(median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group)
# View the results
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp = world %>%
group_by(continent) %>%       # Group the data by continent
summarise(median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group)
# View the results
print(median_life_exp)
library(dplyr)
# Assuming the world dataset is already loaded. If not, you would need to load it with data(world) or similar.
median_life_exp = world %>%
group_by(continent) %>%       # Group the data by continent
summarise(median_life_expectancy = median(lifeExp)  # Calculate the median life expectancy for each group
)
# View the results
print(median_life_exp)
library(dplyr)
median_life_exp = world %>%
group_by(continent) %>%
summarise(median_life_expectancy = median(lifeExp)
)
# View the results
print(median_life_exp)
library(dplyr)
median_life_exp = world %>%
group_by(continent) %>%
summarise(median_life_expectancy = median(lifeExp)
)
print(median_life_exp)
